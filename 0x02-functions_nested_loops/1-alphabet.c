#include<stdio.h>
/**
 * main -  function that prints the alphabet, in lowercase
 * return 0
 */

void print_alphabet(void)
{
	int i;
	print_alphabet();
		for(i=97;i<=122;i++)
		{
			putchar(i);

		}
		putchar('\n');
}
